<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".PlaylistMakerApp"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlaylistMaker"
        tools:targetApi="31">

        <!-- Главный экран -->
        <activity
            android:name=".feature_main.presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.PlaylistMaker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Поиск -->
        <activity
            android:name=".feature_search.presentation.SearchActivity"
            android:theme="@style/Theme.PlaylistMaker" />

        <!-- Плеер -->
        <activity
            android:name=".feature_player.presentation.PlayerActivity"
            android:theme="@style/Theme.PlaylistMaker" />

        <!-- Настройки -->
        <activity
            android:name=".feature_settings.presentation.SettingsActivity"
            android:theme="@style/Theme.PlaylistMaker" />

    </application>

</manifest>